{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Git Add, Commit, and Push",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-Command",
                "& { git rm -r --ched .ac ; git add .; git commit -m \"Initial commit\"; git push -u origin main }"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "A custom task to add, commit, and push changes to GitHub in PowerShell."
        }
    ]
}
